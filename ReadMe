GuidesLocal is a simple ios7 app that keeps a list of documents in a UITableView and allows a user to create, edit and save new documents to their local disk.  It implements NSFileWrappers for adding an image to the document at a later date.  It works for both iPhone and iPad.

